---
import Layout from "../layouts/Layout.astro";
import FeaturedShowcase from "../components/FeaturedShowcase";
import { getCollection } from "astro:content";
import fs from "fs";
import path from "path";
import HomeHero from "../components/HomeHero";
import HomepageSectionControls from "../components/HomepageSectionControls";

const projects = await getCollection("projects");
const sortedProjects = projects.sort(
    (a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf(),
);

// Load featured projects from settings
let featuredProjects = sortedProjects.slice(0, 3); // Default fallback

try {
    const settingsPath = path.join(
        process.cwd(),
        "src/content/settings/homepage.json",
    );
    const settingsContent = fs.readFileSync(settingsPath, "utf-8");
    const settings = JSON.parse(settingsContent);

    if (settings.featuredProjects && settings.featuredProjects.length > 0) {
        // Featured projects are stored as simple slug strings
        const featuredSlugs: string[] = settings.featuredProjects;

        // Find matching projects and preserve the order from settings
        const matchedProjects = featuredSlugs
            .map((slug) => projects.find((p) => p.id === slug))
            .filter((p): p is (typeof projects)[number] => p !== undefined);

        if (matchedProjects.length > 0) {
            featuredProjects = matchedProjects;
        }
    }
} catch (e) {
    // Settings file not found or invalid, use default (latest 3)
    console.log("Using default featured projects (latest 3)");
}
const heroImage = featuredProjects[0]?.data.mainImage;
const heroFallback = {
  eyebrow_fr: "Pâtissier · Poitiers · France", title_fr: "Je veux faire du dessert le dernier souvenir.", body_fr: "Je travaille les desserts de restaurant, les entremets et les créations de saison. Je construis ce portfolio au fil de ma formation et de mes expériences.",
  eyebrow_en: "Pastry chef · Poitiers · France", title_en: "I want dessert to become the lasting memory.", body_en: "I create restaurant desserts, entremets and seasonal pastries. This portfolio grows alongside my training and professional experience.",
  hero_image_url: typeof heroImage === "string" ? heroImage : "/uploads/IMG_1882.jpeg", hero_image_alt_fr: featuredProjects[0]?.data.title ?? "Création pâtissière de Florent Dabert", hero_image_alt_en: featuredProjects[0]?.data.titleEn ?? "Florent Dabert pastry creation",
};
const featuredFallback = featuredProjects.map((project) => ({ id: project.id, title_fr: project.data.title, title_en: project.data.titleEn || null, description_fr: project.data.description || null, description_en: project.data.descriptionEn || null, main_image_url: typeof project.data.mainImage === "string" ? project.data.mainImage : null, origin: project.data.origin || null }));
---

<Layout title="Pâtissier · desserts de restaurant" description="Je suis Florent Dabert, pâtissier en formation spécialisé en desserts de restaurant et créations pâtissières.">
  <HomeHero client:load locale="fr" fallback={heroFallback} />
  <FeaturedShowcase client:load locale="fr" fallback={featuredFallback} />
  <section data-home-section="journey" class="mx-auto grid max-w-7xl gap-8 px-4 py-16 sm:px-6 md:grid-cols-3 lg:px-8"><div><p class="eyebrow">Mon parcours évolue</p><h2 class="mt-3 font-serif text-3xl text-stone-950">Apprendre, créer, voyager.</h2></div><p class="text-stone-600">Je prépare actuellement un Certificat de spécialisation Desserts de restaurant au Campus 120. J’y développe une pâtisserie de précision, pensée pour l’assiette, avec l’envie de poursuivre mon parcours à l’international.</p><a href="/contact" class="button-primary self-start md:justify-self-end">Me contacter</a></section>
  <HomepageSectionControls client:load />
</Layout>
